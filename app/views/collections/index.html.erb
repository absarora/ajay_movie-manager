<div class="container">
  <h1>All Collections</h1>

  <% if policy(Collection.new).create? %>
    <p><%= link_to "New Collection", new_collection_path, class: 'btn btn-primary' %></p>
  <% end %>
  <% @collections.each do |collection| %>
    <p><%= link_to collection.title, collection, data: { no_turbolink: true } %></p>
    <small>
      updated <%= time_ago_in_words(collection.updated_at) %> ago by <%= collection.user.name %>
      <%= collection.movies.count %> Movies
    </small>
  <% end %>
</div>